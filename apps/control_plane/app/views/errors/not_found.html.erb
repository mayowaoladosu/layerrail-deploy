<% content_for :title, "Not found · LayerRail Deploy" %>

<%= render "shared/legacy_header" if current_principal %>

<main id="main-content" class="container max-w-screen-xl mx-auto p-8">
  <section class="flex items-center justify-center min-h-60 bg-card text-card-foreground border shadow-sm rounded-xl p-4" aria-labelledby="not-found-heading">
    <div class="max-w-sm text-center"><h1 class="h4" id="not-found-heading">That deployment is not available.</h1><p class="text-muted-foreground mt-1">It may have been removed or belong to another team.</p><%= link_to "Return home", root_path, class: "btn-sm mt-4" %></div>
  </section>
</main>
