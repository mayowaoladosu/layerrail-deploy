<% content_for :title, "Deployment #{@deployment.id.first(7)} · LayerRail Deploy" %>
<%= render "shared/legacy_header" %>

<main id="main-content" class="container max-w-screen-xl mx-auto p-8 space-y-6">
  <div id="deployment-page-header" class="space-y-6">
    <%= render "deployments/header" %>
  </div>

  <%= render "deployments/failure" %>

  <%= render "deployments/live" %>

  <%= render "deployments/history" %>
</main>
